document.addEventListener('DOMContentLoaded', () => {
    /* 
    1 add event listener to each square
    2 when square is clicked set square to appropriate immage
        -run check on class, if does does include "square"
            replace square element with appriate immage, set class to
            said immage.
    */

    
})